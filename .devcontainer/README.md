# Devcontainer használat

VS Code:
- “Dev Containers: Reopen in Container”

CLI build:
- cmake --preset Debug
- cmake --build --preset Debug

Release:
- cmake --preset Release
- cmake --build --preset Release
